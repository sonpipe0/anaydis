package anaydis.search;

/**
 * <p>This enumeration represents a {@link PriorityQueue map} type.
 * each map must return a not null valid {@link PriorityType type}</p>
 *
 * @see HeapPriorityQueue#getType
 */
public enum PriorityType {
    //~ Enum constants .........................................................................................
    HEAP,
    ORDERED,
    UNORDERED


}
